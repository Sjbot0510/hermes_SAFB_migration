\chapter{Crystallographic Background}

\section{Space Groups}

A space group is a mathematical description of the symmetry of a crystal structure. It consists of all symmetry operations (rotations, reflections, inversions, and translations) that leave the crystal invariant.

\subsection{Symmetry Operations}

Each symmetry operation consists of a rotation matrix $\Rmat$ and a fractional translation vector $\mathbf{t}$:

\begin{equation}
    \mathbf{r}' = \Rmat \cdot \mathbf{r} + \mathbf{t}
\end{equation}

where $\mathbf{r}$ is a position vector in direct space.

For 3D space groups, $\Rmat$ is a $3 \times 3$ integer matrix and $\mathbf{t}$ is a 3-vector of fractions. For 2D wallpaper groups, $\Rmat$ is a $2 \times 2$ integer matrix and $\mathbf{t}$ is a 2-vector of fractions (promoted to 3D with z-invariance).

The multiplication of two operations follows:
\begin{equation}
    C = AB: \quad \Rmat_C = \Rmat_A \Rmat_B, \quad \mathbf{t}_C = \mathbf{t}_A + \Rmat_A \mathbf{t}_B
\end{equation}

The inverse of an operation $(\Rmat, \mathbf{t})$ is:
\begin{equation}
    (\Rmat, \mathbf{t})^{-1} = (\Rmat^{-1}, -\Rmat^{-1}\mathbf{t})
\end{equation}

\subsection{Point Group vs. Space Group}

\begin{itemize}
    \item The \textbf{point group} consists only of the rotation/reflection matrices (no translations). It describes the rotational symmetry.
    \item The \textbf{space group} includes both rotations and translations. It describes the full symmetry.
    \item The order of the point group (number of rotation matrices) is always a divisor of the order of the space group (number of symmetry operations).
\end{itemize}

\section{Bravais Lattices}

\subsection{The Five 2D Bravais Lattices}

There are exactly five distinct Bravais lattice types in two dimensions, classified by their metric properties (lattice parameters $a$, $b$, $\gamma$):

\begin{enumerate}
    \item \textbf{Oblique}: $a \neq b$, $\gamma \neq 90^\circ$. No symmetry constraints. The most general 2D lattice.
    \item \textbf{Rectangular Primitive}: $a \neq b$, $\gamma = 90^\circ$. Orthogonal basis with unequal lengths.
    \item \textbf{Rectangular Centered}: $a \neq b$, $\gamma = 90^\circ$. Additional lattice point at $(a/2, b/2)$. Can be described by a primitive cell with basis vectors $(a/2, -b/2)$ and $(a/2, b/2)$.
    \item \textbf{Square}: $a = b$, $\gamma = 90^\circ$. Equal orthogonal basis vectors.
    \item \textbf{Hexagonal}: $a = b$, $\gamma = 120^\circ$. Equal basis vectors at $120^\circ$.
\end{enumerate}

\subsection{Direct Lattice Basis Vectors}

The direct lattice is defined by basis vectors $\mathbf{a}_1, \mathbf{a}_2$ (2D) or $\mathbf{a}_1, \mathbf{a}_2, \mathbf{a}_3$ (3D). Any lattice point is:
\begin{equation}
    \mathbf{R} = n_1 \mathbf{a}_1 + n_2 \mathbf{a}_2 \quad \text{(2D)}
\end{equation}
where $n_1, n_2$ are integers.

The unit cell area in 2D is:
\begin{equation}
    A = |\mathbf{a}_1 \times \mathbf{a}_2| = \sqrt{\det(\Gmat)}
\end{equation}

\subsection{Metric Matrix}

The metric matrix $\Gmat$ encodes the geometry of the direct lattice:
\begin{equation}
    G_{ij} = \mathbf{a}_i \cdot \mathbf{a}_j
\end{equation}

In 2D:
\begin{equation}
    \Gmat = \begin{pmatrix}
    \mathbf{a}_1 \cdot \mathbf{a}_1 & \mathbf{a}_1 \cdot \mathbf{a}_2 \\
    \mathbf{a}_2 \cdot \mathbf{a}_1 & \mathbf{a}_2 \cdot \mathbf{a}_2
    \end{pmatrix} = \begin{pmatrix}
    a^2 & ab\cos\gamma \\
    ab\cos\gamma & b^2
    \end{pmatrix}
\end{equation}

The unit cell area is $A = \sqrt{\det(\Gmat)} = ab\sin\gamma$.

\subsection{Transformation of the Metric Matrix}

Under a change of basis $\mathbf{a}'_i = \sum_j \mathbf{a}_j P_{ji}$, the metric matrix transforms as:
\begin{equation}
    \Gmat' = \Pmat^T \Gmat \Pmat
\end{equation}
where $\Pmat$ is the transformation matrix. The ratio of cell areas is $A'/A = |\det(\Pmat)|$.

\section{Reciprocal Lattice}

\subsection{Reciprocal Basis Vectors}

The reciprocal lattice is the Fourier transform of the direct lattice. For a direct lattice with basis vectors $\mathbf{a}_1, \mathbf{a}_2, \mathbf{a}_3$, the reciprocal lattice basis vectors are:

\begin{equation}
    \mathbf{b}_1 = 2\pi \frac{\mathbf{a}_2 \times \mathbf{a}_3}{\mathbf{a}_1 \cdot (\mathbf{a}_2 \times \mathbf{a}_3)}, \quad
    \mathbf{b}_2 = 2\pi \frac{\mathbf{a}_3 \times \mathbf{a}_1}{\mathbf{a}_1 \cdot (\mathbf{a}_2 \times \mathbf{a}_3)}, \quad
    \mathbf{b}_3 = 2\pi \frac{\mathbf{a}_1 \times \mathbf{a}_2}{\mathbf{a}_1 \cdot (\mathbf{a}_2 \times \mathbf{a}_3)}
\end{equation}

The defining property is:
\begin{equation}
    \mathbf{a}_i \cdot \mathbf{b}_j = 2\pi \delta_{ij}
\end{equation}

\subsection{Reciprocal Lattice Vectors for Each 2D Bravais Lattice}

\begin{table}[h]
\centering
\begin{tabular}{lll}
\hline\hline
\textbf{Lattice Type} & \textbf{Direct Basis} & \textbf{Reciprocal Basis} \\
\hline
Oblique & $\mathbf{a}_1 = a(1,0)$, $\mathbf{a}_2 = a(\cos\gamma, \sin\gamma)$ & $\mathbf{b}_1 = \frac{2\pi}{a\sin\gamma}(1, -\cot\gamma)$, $\mathbf{b}_2 = \frac{2\pi}{a\sin\gamma}(0, 1)$ \\
Rectangular & $\mathbf{a}_1 = a(1,0)$, $\mathbf{a}_2 = b(0,1)$ & $\mathbf{b}_1 = \frac{2\pi}{a}(1,0)$, $\mathbf{b}_2 = \frac{2\pi}{b}(0,1)$ \\
Centered Rect. & $\mathbf{a}_1 = (a/2,-b/2)$, $\mathbf{a}_2 = (a/2,b/2)$ & $\mathbf{b}_1 = \frac{2\pi}{a}(1,1)$, $\mathbf{b}_2 = \frac{2\pi}{a}(1,-1)$ \\
Square & $\mathbf{a}_1 = a(1,0)$, $\mathbf{a}_2 = a(0,1)$ & $\mathbf{b}_1 = \frac{2\pi}{a}(1,0)$, $\mathbf{b}_2 = \frac{2\pi}{a}(0,1)$ \\
Hexagonal & $\mathbf{a}_1 = a(1,0)$, $\mathbf{a}_2 = a(1/2,\sqrt{3}/2)$ & $\mathbf{b}_1 = \frac{2\pi}{a}(1,-1/\sqrt{3})$, $\mathbf{b}_2 = \frac{2\pi}{a}(0,2/\sqrt{3})$ \\
\hline\hline
\end{tabular}
\caption{Direct and reciprocal basis vectors for the five 2D Bravais lattices.}
\end{table}

\subsection{Reciprocal Lattice Metric}

The reciprocal metric tensor is:
\begin{equation}
    G^*_{ij} = \mathbf{b}_i \cdot \mathbf{b}_j
\end{equation}

It is related to the direct metric by $\Gmat^* = (\Gmat^{-1})^T$ (or equivalently $\Gmat^* = \Gmat^{-1}$ since $\Gmat$ is symmetric).

\subsection{d-Spacing and Miller Indices}

A reciprocal lattice vector is labeled by Miller indices $(h, k)$:
\begin{equation}
    \mathbf{q} = h\mathbf{b}_1 + k\mathbf{b}_2
\end{equation}

The d-spacing (interplanar distance) is:
\begin{equation}
    d_{hk} = \frac{2\pi}{|\mathbf{q}|} = \frac{2\pi}{\sqrt{\mathbf{q}^T \Gmat^* \mathbf{q}}}
\end{equation}

\section{The 17 Wallpaper Groups}

\subsection{Overview}

There are exactly 17 wallpaper groups — the distinct symmetry groups of 2D periodic patterns. They are classified into five crystal systems based on their underlying Bravais lattice:

\begin{table}[h]
\centering
\begin{tabular}{lll}
\hline\hline
\textbf{Crystal System} & \textbf{Bravais Lattice} & \textbf{Wallpaper Groups} \\
\hline
Oblique & Oblique & p1, p2 \\
Rectangular Primitive & Rectangular Primitive & pm, pg, p2mm, p2mg, p2gg \\
Rectangular Centered & Rectangular Centered & cm, c2mm \\
Square & Square & p4, p4mm, p4gm, p422 \\
Hexagonal & Hexagonal & p3, p3m1, p31m, p6, p6mm \\
\hline\hline
\end{tabular}
\caption{Classification of the 17 wallpaper groups by crystal system.}
\end{table}

\subsection{Symmetry Operations by Group}

\begin{table}[h]
\centering
\small
\begin{tabular}{lll}
\hline\hline
\textbf{Group} & \textbf{Operations} & \textbf{Order} \\
\hline
p1 & Identity only & 1 \\
p2 & Identity, 2-fold rotation & 2 \\
pm & Identity, mirror reflection & 2 \\
pg & Identity, glide reflection & 2 \\
cm & Identity, centered mirror & 2 \\
p2mm & Identity, 2-fold, 2 mirrors, inversion & 4 \\
p2mg & Identity, 2-fold, mirror, glide & 4 \\
p2gg & Identity, 2-fold, 2 glides & 4 \\
c2mm & Identity, 2-fold, 2 mirrors (centered) & 4 \\
p4 & Identity, 4-fold, 2-fold rotations & 4 \\
p4mm & Identity, 4-fold, mirrors & 8 \\
p4gm & Identity, 4-fold, mirrors, glides & 8 \\
p422 & Identity, 4-fold, 2-folds (all directions) & 8 \\
p3 & Identity, 3-fold rotations & 3 \\
p3m1 & Identity, 3-fold, mirrors through vertices & 6 \\
p31m & Identity, 3-fold, mirrors between vertices & 6 \\
p6 & Identity, 6-fold rotations & 6 \\
p6mm & Identity, 6-fold, mirrors & 12 \\
\hline\hline
\end{tabular}
\caption{Symmetry operations for each wallpaper group.}
\end{table}

\subsection{Crystallographic Restriction Theorem}

Only n-fold rotations with $n = 1, 2, 3, 4, 6$ are compatible with translational periodicity in 2D. This is the \textbf{crystallographic restriction theorem}.

\subsection{Asymmetric Unit}

The \textbf{asymmetric unit} is the smallest region of the unit cell that, under all symmetry operations of the group, generates the entire pattern. The area of the asymmetric unit is the unit cell area divided by the order of the group.

\section{Stars}

A \textbf{star} of a reciprocal lattice vector $\mathbf{q}$ is the set of all vectors generated by applying the point group operations:

\begin{equation}
    \text{Star}(\mathbf{q}) = \{ \Rmat \cdot \mathbf{q} \mid \Rmat \in \text{Point Group} \} \cup \{ -\Rmat \cdot \mathbf{q} \mid \Rmat \in \text{Point Group} \}
\end{equation}

The union with negatives ensures the star is closed under inversion, which is required for real-valued Fourier series.

\subsection{Star Properties}

\begin{itemize}
    \item \textbf{Multiplicity}: The number of vectors in the star. Determined by the order of the point group divided by the order of the stabilizer subgroup.
    \item \textbf{Closure}: A star is closed if it contains all symmetry-equivalent vectors. The function \texttt{star\_is\_closed()} verifies this.
    \item \textbf{q-squared}: All vectors in a star have the same $|\mathbf{q}|^2$ (q-squared), since rotations preserve length.
\end{itemize}

\section{Miller Indices}

Miller indices $(h, k, l)$ label reciprocal lattice vectors and crystallographic planes. In the SAFB library, Miller indices are used to:

\begin{itemize}
    \item Identify and group reciprocal lattice vectors into stars
    \item Compute the canonical key for lexicographic sorting
    \item Label modes in the output
\end{itemize}

The canonical key is computed by sorting the Miller indices and taking the absolute value of the first non-zero element, ensuring a unique identifier for each family of planes.
